

  <article class="row">
    <div class="col-sm-3">
        <a href="#families/{{family.familyId}}">
          <div class="text-left text-blue">{{family.name}}</div>
        </a>
    </div>
    <div class="col-sm-8">
      <p class="card-text">{{family.code}}</p>
    </div>
    <div class="col-sm-1">
        <a class="card-menu-item card-menu-delete" href="#" title="{{$t 'family.manage.select-delete'}}" style="margin:0 auto"></a>
    </div>
  </article>
